name: Run PDF White-Box Bot

on:
  push:
    branches: [ "main" ]     # يشتغل أول ما ترفع الكود
  workflow_dispatch:         # يضيف لك زر تشغيل يدوي
  schedule:
    - cron: '0 */5 * * *'    # ⚠️ أهم سطر: يعيد التشغيل كل 5 ساعات تلقائياً

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 350     # يقتل العملية قبل الـ 6 ساعات (عشان يشتغل الجدول الزمني الجديد)

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Bot Script
      env:
        # 🔴 لازم تكون ضايف التوكين في إعدادات الأمان (Secrets)
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      run: |
        python bot.py
